#include <iostream>

#define N_LAYER (8)
#define N_MESHX (8)
#define N_MESHY (8)
#define N2_MESH (N_MESHX*N_MESHY)
#define TINY (1.0e-31)
#define PRINT_ARRAY(array) for(size_t i=0; i<(int)sizeof(array); i++) cout << *array[i] << " "; cout << endl;

using namespace std;

int main() {
    int spm[N_LAYER] = 

    
    return 0;
}
